{{!< default}}

<div class="tag-header">
  <h1>#{{tag.name}}</h1>
  {{#if tag.description}}
    <p>{{tag.description}}</p>
  {{/if}}
</div>

<div class="content-area">
  <main>
    {{#foreach posts}}
      {{> post-card}}
    {{/foreach}}

    {{> pagination}}
  </main>

  {{> sidebar}}
</div>
