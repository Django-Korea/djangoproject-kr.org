{{!< default}}

<div class="author-header">
  {{#if author.profile_image}}
    <img src="{{author.profile_image}}" alt="{{author.name}}">
  {{/if}}
  <h1>{{author.name}}</h1>
  {{#if author.bio}}
    <p style="color: #666; max-width: 600px; margin: 0 auto;">{{author.bio}}</p>
  {{/if}}
</div>

<div class="content-area">
  <main>
    {{#foreach posts}}
      {{> post-card}}
    {{/foreach}}

    {{> pagination}}
  </main>

  {{> sidebar}}
</div>
